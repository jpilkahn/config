[core]
    autocrlf = input
    editor = joe
    excludesfile = ~/.gitconfig.d/excludesfile

[gpg]
    program = gpg2

[apply]
    whitespace = nowarn

[branch]
    autosetupmerge = always
    autosetuprebase = always

[diff]
    mnemonicprefix = true
#    external = git-meld

[push]
    default = upstream

[pull]
    default = current

[rebase]
    autosquash = true
