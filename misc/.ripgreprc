--colors=line:style:bold

--column

--after-context=2
--before-context=2

--follow

--glob
!.git/*

--glob
!.pnp?(.data).js

--glob
!.yarn/*

--glob
!yarn.lock

--hidden

--line-number

--max-columns=150
--max-columns-preview

--smart-case

--type-add
bundler:{rollup,webpack}.config.json

--type-add
lockfile:{composer,package,yarn}.lock

--type-add
manifest:{composer,package}.json

--type-add
rollup:rollup.config.json

--type-add
web:*.{css,html,js}

--type-add
webpack:webpack.config.json
